diff --git a/package.json b/package.json
--- a/package.json
+++ b/package.json
@@
-    "@supabase/ssr": "<remove>",
-    "@supabase/supabase-js": "<remove>",
+    // Remove unused Supabase runtime dependencies.
+    // Regenerate package-lock.json during the approved implementation step.

diff --git a/Dockerfile b/Dockerfile
--- a/Dockerfile
+++ b/Dockerfile
@@
-ARG NEXT_PUBLIC_SUPABASE_URL
-ARG NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY
@@
-ENV NEXT_PUBLIC_SUPABASE_URL=$NEXT_PUBLIC_SUPABASE_URL
-ENV NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=$NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY
+# Supabase public build variables are intentionally removed for the
+# self-hosted PostgreSQL staging target.

diff --git a/.github/workflows/staging-ghcr.yml b/.github/workflows/staging-ghcr.yml
--- a/.github/workflows/staging-ghcr.yml
+++ b/.github/workflows/staging-ghcr.yml
@@
-      STAGING_NEXT_PUBLIC_SUPABASE_URL
-      STAGING_NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY
+      # Remove unused Supabase public variables from staging image build
+      # validation and Docker build args.

diff --git a/src/lib/supabase.ts b/src/lib/supabase.ts
deleted file mode 100644
--- a/src/lib/supabase.ts
+++ /dev/null
@@
-// Delete unused Supabase browser/server client entrypoint.

diff --git a/src/utils/supabase/client.ts b/src/utils/supabase/client.ts
deleted file mode 100644
--- a/src/utils/supabase/client.ts
+++ /dev/null
@@
-// Delete unused Supabase browser client factory.

diff --git a/src/utils/supabase/server.ts b/src/utils/supabase/server.ts
deleted file mode 100644
--- a/src/utils/supabase/server.ts
+++ /dev/null
@@
-// Delete unused Supabase server client factory.

diff --git a/src/utils/supabase/middleware.ts b/src/utils/supabase/middleware.ts
deleted file mode 100644
--- a/src/utils/supabase/middleware.ts
+++ /dev/null
@@
-// Delete unused Supabase middleware helper.

diff --git a/next.config.ts b/next.config.ts
--- a/next.config.ts
+++ b/next.config.ts
@@
       {
         protocol: "https",
         hostname: "tygjmrhandbffjllxveu.supabase.co",
         pathname: "/storage/v1/object/public/**",
       },
+      // Keep this legacy Supabase Storage hostname for now. Local source/data
+      // scan did not find stored Supabase media URLs, but that is not enough
+      // evidence to prove production rows no longer reference old images.

diff --git a/docs/deploy/staging-coolify.md b/docs/deploy/staging-coolify.md
--- a/docs/deploy/staging-coolify.md
+++ b/docs/deploy/staging-coolify.md
@@
-# Supabase-backed staging runtime variables
+# Self-hosted PostgreSQL staging runtime variables
@@
-SUPABASE_SERVICE_ROLE_KEY
-NEXT_PUBLIC_SUPABASE_URL
-NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY
+DATABASE_URL
+DIRECT_URL
+# Keep Bunny CDN variables for file/image delivery.

diff --git a/README.md b/README.md
--- a/README.md
+++ b/README.md
@@
-NEXT_PUBLIC_SUPABASE_URL
-NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY
+# Supabase public runtime variables are not required for the self-hosted
+# PostgreSQL staging target.

Implementation notes:

- This patch is intentionally illustrative and must be converted into a real
  source diff only after approval.
- `package-lock.json` must be regenerated in the approved source-change branch.
- Old Supabase operator scripts under `scripts/` should be either rewritten,
  archived, or clearly marked legacy before production go-live if they are no
  longer part of operations.
- The Supabase Storage image host in `next.config.ts` is intentionally kept in
  the actual source diff until production media URLs are verified or migrated.
