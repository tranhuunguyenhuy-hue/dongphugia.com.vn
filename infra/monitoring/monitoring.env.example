# Replace with exact production container names after a read-only digest check.
# Leave empty until the operator has verified the allowlist; an empty value
# intentionally emits monitoring_configuration_missing and exits non-zero.
DPG_MONITOR_CONTAINERS=
DPG_MONITOR_WINDOW_SECONDS=90
