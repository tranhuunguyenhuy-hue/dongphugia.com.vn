# Scripts

For Hita product crawl/import work, use only the normalized brand pipeline:

```bash
npx tsx scripts/crawl-hita/run-normalized-brand-pipeline.mts
```

Read the full operating procedure in [scripts/crawl-hita/README.md](/Users/m-ac/Projects/dongphugia/scripts/crawl-hita/README.md).

Legacy per-brand crawlers and old Hita phase scripts are not supported for new sessions.
